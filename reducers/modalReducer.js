import { SHOW_DETAIL } from "../action/types";
const initialState = {
  Detail: {}
};
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
